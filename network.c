#include <stdlib.h>
#include <stdio.h>

void CalculeLayer(float *layer[])
{
    int len = sizeof(&layer) / sizeof(&layer[0]);

    for (int i = 0; i < len; i++)
    {
      
    }
}

int main(){
    printf("Ok!");
}